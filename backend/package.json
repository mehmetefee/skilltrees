{
  "name": "skilltree-backend",
  "version": "1.0.0",
  "description": "Backend API for public skill tree sharing site (zero external deps: uses node:http and node:sqlite)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "seed": "node db/seed.js",
    "test": "node --test \"../tests/api/*.test.js\""
  }
}
